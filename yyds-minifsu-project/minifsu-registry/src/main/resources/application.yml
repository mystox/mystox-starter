logging:
  level:
   root: info

server:
  name: minifsu-alarm
  bindIp: 127.0.0.1
  rpc:
    port: 18880


executor:
  threadPool:
    corePoolSize: 10 #核心执行线程数
    maxPoolSize: 1000 #最大维护线程数


rpc:
  client:
    timeout: 20000
mqtt:
  username: root
  password: 123456
  url: tcp://172.16.5.26:1883 # 推送信息的连接地址，如果有多个，用逗号隔开，如：tcp://127.0.0.1:61613,tcp://192.168.1.61:61613
  producer:
    clientId: ${spring.application.name}_producer
    defaultTopic: topic
  completionTimeout: 3000
  consumer:
    clientId: ${spring.application.name}_consumer
    defaultTopic: topic
mqtt:
  producer:
    clientId: ${server.groupCode}_${server.name}_${server.version}_producer
    defaultTopic: topic
  completionTimeout: 3000
  consumer:
    clientId: ${server.groupCode}_${server.name}_${server.version}_consumer
    defaultTopic: topic
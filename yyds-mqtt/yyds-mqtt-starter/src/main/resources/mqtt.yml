mqtt:
  producer:
    clientId: ${server.group}_${server.name}_${server.version}_producer
    defaultTopic: topic
  completionTimeout: 3000
  consumer:
    clientId: ${server.group}_${server.name}_${server.version}_consumer
    defaultTopic: topic